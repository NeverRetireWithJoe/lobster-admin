{
  "installCommand": "cd frontend && npm install",
  "buildCommand": "cd frontend && npm run build && cp -r dist/* ../public/",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "framework": null
}
